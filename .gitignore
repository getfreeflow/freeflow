# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.moved-aside

# Generated by XcodeGen — regenerate with `xcodegen generate`
FreeFlow.xcodeproj/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Local code signing identity — machine specific, never committed.
Support/Signing.local.xcconfig

# Never commit secrets. The Groq API key lives in Application Support,
# never in this repo — but keep these ignored just in case.
*.env
secrets.json
credentials.json

# The landing page lives in getfreeflow/getfreeflow.github.io
docs/
